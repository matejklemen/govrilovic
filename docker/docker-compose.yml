version: '3.7'

services:
  db:
    build: .
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: crawldb
    volumes:
      - ../db_data:/var/lib/postgresql/data